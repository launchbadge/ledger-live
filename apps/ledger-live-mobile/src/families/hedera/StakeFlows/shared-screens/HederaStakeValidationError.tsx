// use cosmos's `04-ValidationError | Success` as reference, but try default LLC core `ValidationSuccess | Error` first to see if the message is appropriate
// why? LLD uses the default "success" modal, so .. consistency..